export type TempMovieData = {
    imdbID: string
    title: string
    year: string
    poster: string
}

export type TempWatchedData = {
    imdbID: string
    title: string
    year: string
    poster: string
    runtime: number
    imdbRating: number
    userRating: number
}
